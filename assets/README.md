# Assets

Place sprites, audio, VFX here. For large binaries, enable Git LFS in `.gitattributes` before committing.

Tone axis: spec `docs/context/CreativeDirection_WardGate.md`, `docs/world/framework/WRD-001_ContentAxisAndTagging.md`.
